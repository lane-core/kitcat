Integers from the Agda builtin.

```agda

{-# OPTIONS --safe --cubical-compatible --no-guardedness #-}

module Core.Data.Int.Type where

open import Agda.Builtin.Int public hiding (module Int)
open Agda.Builtin.Int.Int public

```
