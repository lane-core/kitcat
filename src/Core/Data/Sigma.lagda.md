Dependent pairs: type and operations.

```agda

{-# OPTIONS --safe --cubical-compatible --no-guardedness #-}

module Core.Data.Sigma where

open import Core.Data.Sigma.Type public

module Σ where
  open import Core.Data.Sigma.Base public

```
