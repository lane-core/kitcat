The `Word64` type from Agda builtins.

```agda

{-# OPTIONS --safe --cubical-compatible --no-guardedness #-}

module Core.Prim.Word64.Type where

open import Agda.Builtin.Word public
  using (Word64)

```
