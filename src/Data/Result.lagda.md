Dependent tagged pairs: type and operations.

```agda

{-# OPTIONS --safe --cubical-compatible --no-guardedness #-}

module Data.Result where

open import Data.Result.Type public hiding (module Res)

module Res where
  open import Data.Result.Base public

```
